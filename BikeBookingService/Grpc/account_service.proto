syntax = "proto3";

service AccountServiceGrpc {
  rpc GetAccountPoint (GetAccountPointRequest) returns (GetAccountPointResponse);
}

message GetAccountPointRequest {
  string accountEmail = 1;
}

message GetAccountPointResponse {
  double point = 1;
}
